<h1>Items</h1>

<% @items.each do |item| %>
  <div id="item-<%= item.id %>">
    <h3><%= item.name %></h3>
    <p>Price: $<%= item.price %></p>
    <p>In Stock at <%= item.supermarket_name %></p>
    <p>Purchased by <%= item.customer_purchase_count %> customers</p>
  </div>
<% end %>